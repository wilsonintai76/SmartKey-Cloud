# Production API endpoints — used by Vite build for Capacitor native mode
# WebAuthn uses relative paths (same-origin via Pages Function proxy)
# Non-WebAuthn APIs use the full Worker URL for native app
VITE_AUDIT_API=https://smartkey-production.wilson-b6f.workers.dev/api/audit
VITE_CLOUD_API=https://smartkey-production.wilson-b6f.workers.dev/api/sync
